import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    MainContainer: {
        flex: 1,
        justifyContent: 'center',
        marginTop: 20,
    },
    imageThumbnail: {
        justifyContent: 'center',
        alignItems: 'center',
        height: 100
    },
    listTitle: {
        position: 'absolute',
        color: '#111',
        fontSize: 18,
        fontWeight: '700',
        marginLeft: 15
    },
});